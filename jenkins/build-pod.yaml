apiVersion: v1
kind: Pod
spec:
  containers:
  - name: node
    image: node:14.18.1
    resources:
      requests:
        cpu: 0.8
    command:
    - cat
    tty: true
  imagePullSecrets:
    - name: dockerhub
  nodeSelector:
    instance-type: spot
